# Base runtime dependencies (mirrors pyproject.toml)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
torch>=2.1
joblib>=1.3
matplotlib>=3.7
shap>=0.45

# Install the project itself in editable mode (provides package entry points)
-e .
